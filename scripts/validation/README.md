# Validation scripts

Run `python3 scripts/validation/validate_repository.py` before database checks.
The validator checks the required repository structure, migration names,
manifests, Supabase project identity and forbidden local artefacts.
