# Release scripts

Release automation is intentionally limited in version 0.1.0. Follow
`docs/release-process.md`; no script in this directory deploys or links a hosted
Supabase project.

Repository validation includes the reviewed standard hub-manifest registry;
the import unit suite is a separate required release gate.
