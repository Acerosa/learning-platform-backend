# Fixtures

Local database fixtures are defined in `supabase/seed.sql`. They use synthetic
Auth users, learners, staff, groups, hub registrations and health records only.

Never place exported production learner or staff data in this directory.
